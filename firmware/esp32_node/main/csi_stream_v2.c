#include "csi_frame_v2.h"

/*
 * Phase-1 firmware stub.
 * The live implementation should:
 * 1) Fill header fields in network byte order.
 * 2) Fill payload from wifi CSI callback data.
 * 3) Send only (header + payload prefix + csi_len bytes) over UDP.
 */

int csi_stream_v2_placeholder(void) {
    return 0;
}
